# Default: relative /api — Next.js proxies to backend (recommended for ngrok)
NEXT_PUBLIC_API_URL=/api
BACKEND_INTERNAL_URL=http://127.0.0.1:8001

# Optional: separate ngrok tunnel for API (set frontend ngrok URL in .env CORS_ORIGINS)
# NEXT_PUBLIC_API_URL=https://your-api.ngrok-free.app/api
