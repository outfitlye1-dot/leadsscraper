# Frontend talks to backend via this URL in the browser.
# Local (Next.js proxies /api → backend):
NEXT_PUBLIC_API_URL=/api
BACKEND_INTERNAL_URL=http://127.0.0.1:8001

# Vercel production (set in Vercel dashboard):
# NEXT_PUBLIC_API_URL=https://YOUR-RAILWAY.up.railway.app/api
